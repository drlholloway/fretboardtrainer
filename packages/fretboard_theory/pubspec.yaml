name: fretboard_theory
description: Pure-Dart music theory for fretted instruments. Notes, tunings, instruments, chord voicings and naming, drill question generation, and the learning curriculum. No I/O, no Flutter.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  meta: ^1.15.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
